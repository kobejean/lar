name: Test
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]

    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v4

    - name: Setup CMake
      uses: jwlawson/actions-setup-cmake@v2
      with:
        cmake-version: '3.27.x'

    - name: Install dependencies (Linux)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          libgl1-mesa-dev libglu1-mesa-dev freeglut3-dev \
          libeigen3-dev \
          libopenblas-dev \
          liblapack-dev \
          libblas-dev \
          libjpeg-dev \
          libpng-dev \
          pkg-config \
          build-essential \
          cmake \
          git \
          libgrpc++-dev \
          libprotobuf-dev \
          protobuf-compiler \
          protobuf-compiler-grpc \
          libabsl-dev

    - name: Install dependencies (macOS)
      if: runner.os == 'macOS'
      run: |
        brew update
        brew install eigen suite-sparse grpc protobuf

    - name: Update Submodules
      run: git submodule update --init --recursive

    - name: Build
      run: make tests

    - name: Test
      run: ./bin/lar_test
