add_subdirectory(core)
if (NOT LAR_COMPACT_BUILD)
  add_subdirectory(mapping)
  add_subdirectory(processing)
endif()
add_subdirectory(tracking)

if(LAR_BUILD_SERVER)
  add_subdirectory(server)
endif()